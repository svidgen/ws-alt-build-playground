{
  "name": "main",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "esbuild index.ts --bundle --outdir=./dist",
    "watch": "esbuild --watch index.ts --bundle --outdir=./dist"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "generate-api": "*"
  }
}
